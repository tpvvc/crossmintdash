{
  "name": "crossmint-nft-system",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate": "wrangler d1 execute TOIAF_DB --file ./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^3.80.0"
  }
}
